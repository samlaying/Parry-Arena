# Deploy / OpenAI Sites Notes

- Do not paste `STEP_API_KEY` into public repos.
- Use a deploy secret or environment binding for `STEP_API_KEY`.
- The built static demo can be reviewed offline via `/public/demo.html`.
